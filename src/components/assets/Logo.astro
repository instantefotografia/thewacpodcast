---
const { class: className = "", variant = "chocolate", ...rest } = Astro.props;
const src = variant === "white" ? "/WAC-logo-branco.png" : "/WAC-logo-chocolate.png";
---

<img
  src={src}
  alt="Wedding Artists Community"
  class={className}
  {...rest}
/>
