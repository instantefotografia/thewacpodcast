---
// Redireciona slugs antigos do template para a nova rota /blog/[slug]
// Esta página já não é usada — os artigos estão em /blog/[slug].astro
export async function getStaticPaths() {
  return [];
}
---
